services:
  sohl-python-app:
  # image we want to build
    image: sohl-pyapp:samplev1
    # location and name of Dockerfile  
    build:
      context: ./pyapp
      dockerfile: Dockerfile
  sohl-web-app:
    image: sohl-webapp:samplev1
    build: .